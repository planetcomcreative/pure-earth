<head>
    <link rel="Shortcut Icon" type="image/x-icon" href="{{ asset('/favicon.png') }}" />
</head>
<img src="{{ absolute_url(asset('/bundles/nspurearth/images/logo.png')) }}" alt="{{ sitename }}" />
{% block body %}

{% endblock %}
